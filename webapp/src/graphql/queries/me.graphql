query Me {
    me {
        id
        email
        firstname
        lastname
        lastLoginAt
        feed(orderBy: { orderIndex: asc }) {
            id
            orderIndex
            createdAt
            url
        }
    }
}
