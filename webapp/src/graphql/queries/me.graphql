query Me {
    me {
        id
        email
        firstname
        lastname
        lastLoginAt
        _count {
            feed
        }
        feed(orderBy: { orderIndex: asc }) {
            id
            orderIndex
            createdAt
            url
            category {
                id
                name
            }
        }
    }
}
