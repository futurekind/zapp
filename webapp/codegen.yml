overwrite: true
schema: ${REACT_APP_GRAPHQL_ENDPOINT}
documents: 'src/**/*.graphql'
generates:
    src/generated/graphql.ts:
        plugins:
            - 'typescript'
            - 'typescript-operations'
            - 'urql-introspection'
