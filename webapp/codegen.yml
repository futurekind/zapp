overwrite: true
schema: 'http://localhost:3001'
documents: 'src/**/*.graphql'
generates:
    src/generated/graphql.ts:
        plugins:
            - 'typescript'
            - 'typescript-operations'
            - 'urql-introspection'
    ./graphql.schema.json:
        plugins:
            - 'introspection'
